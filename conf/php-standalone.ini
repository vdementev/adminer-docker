; Standalone flavor only — it serves requests from the CLI SAPI, where opcache
; is off by default. Without this every request recompiles the ~380 KB
; adminer.php from scratch. The forked `php -S` workers inherit the shared
; opcache memory from the parent, so they all hit the same warm cache.
[opcache]
opcache.enable_cli = 1
